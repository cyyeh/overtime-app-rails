%tr
  %td
    = post.id
  %td
    = post.overtime_request
  %td
    = post.date
  %td
    = post.user.full_name
  %td
    = link_to truncate(post.rationale), post
  %td
    = status_label(post.status)
  %td
    = link_to '', edit_post_path(post), :id => "edit_#{post.id}", :class => 'glyphicon glyphicon-pencil index-icons' if policy(post).update?
  %td
    = link_to '', post_path(post), method: :delete, :id => "delete_#{post.id}", :class => 'glyphicon glyphicon-trash index-icons', data: { confirm: 'Are you sure?' }    